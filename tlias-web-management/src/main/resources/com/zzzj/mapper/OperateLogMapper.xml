<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzzj.mapper.OperateLogMapper">


    <select id="page" resultType="com.zzzj.pojo.OperateLog">
        SELECT
            ol.id,
            ol.operate_emp_id,
            ol.operate_time,
            ol.class_name,
            ol.method_name,
            ol.method_params,
            ol.return_value,
            ol.cost_time,
            e.name AS operateEmpName
        FROM operate_log ol
        LEFT JOIN emp e ON ol.operate_emp_id = e.id
        ORDER BY ol.operate_time DESC
    </select>
</mapper>